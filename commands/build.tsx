import React from 'react';
import { Box, Text } from 'ink';

interface Props {}

const Build: React.FC<Props> = () => {
  return (
    <Box>
      <Text>build</Text>
    </Box>
  );
};

Build.propTypes = {};

export default Build;
